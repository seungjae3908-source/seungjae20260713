{
  "name": "investment-research-dashboard",
  "version": "1.0.0",
  "private": true,
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "python3 server.py",
    "test": "python3 -m unittest discover -s test -p 'test_*.py' -v",
    "check": "python3 -m py_compile server.py && node --check public/app.js && node --check public/sw.js"
  }
}
