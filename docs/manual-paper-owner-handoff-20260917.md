# EL-CG011 / EL-CG012 / EL-CG013 active owner handoff

The user's 2026-09-17 direct handoff authorizes Draft Paper contract implementation, tests, Fast CI and unchanged bounded-diff revalidation. Current active implementation owner is Codex Paper-contract task `01a0a969-2ab0-7971-b52b-39934162761f`. This task explicitly accepts responsibility for each finding below. Historical #1001 / #891 / #547 authorship is lineage only, not current owner acceptance. Acceptance concerns implementation responsibility, not investment policy or economic validation approval.

| Finding | Active owner | Acceptance | Status |
|---|---|---|---|
| EL-CG011 manual canonical Full Cost consumer | Paper-contract task above | ACCEPTED — direct handoff and this explicit responsibility record | OPEN |
| EL-CG012 manual NetPnL complete cost lineage | Paper-contract task above | ACCEPTED — direct handoff and this explicit responsibility record | OPEN |
| EL-CG013 same-candidate validation receipt consumer | Paper-contract task above | ACCEPTED — direct handoff and this explicit responsibility record | OPEN |

All three remain OPEN. Contract tests do not establish an existing genuine receipt, runtime wiring, or deployed economic evidence. No historical owner is credited with a new acceptance. #1085 retains Scanner/Backtest product wiring ownership under task `01a0ad1c-e395-7540-844a-88a01ac637ff`. Planned files and interface were shared with that active owner; the shared test registry and #1085 branch are preserved.

## Consumer interface

`applyPaperTradingAction(state, action, now, canonicalEvidence?)` reuses the existing manual engine. `ManualPaperCanonicalEvidence` is a server-only fourth argument from an authenticated read-only owner resolver. The actual `/api/paper-trading/evaluate` route accepts an optional `canonicalEvidenceSource` dependency and uses a server-owned clock. Neither HTTP body evidence nor browser timestamps grant canonical authority. The default route has no canonical source and fails closed for canonical requests/positions.

The resolver must read existing account-bound owner state and supply its stable SHA-256 as `paperStateSha256`, not compute authority from request JSON. It must supply a canonical candidate and the actual position generated by `runRecurringPaperCycle`, complete entry cost evidence, the existing same-candidate validation receipt, and its owner-verified durable readback/digest/result. That verification is a trusted backend boundary: callers must connect the existing issuer's verifier/readback, never set verification flags from client data. No publisher, signing key, record, policy, filesystem default or environment configuration is introduced.

For settlement, supply the owner's observation returned by `createNaturalPaperTriggerBoundSettlementCostProducer` and the frozen pending exit. Copying/serializing a valid-looking observation loses the producer capability and fails. Missing, malformed, stale, mismatched, synthetic/replayed/backfilled/test-only receipt data fails; a Backtest reference preview is not a validation receipt. The validation issuer remains an explicit unresolved dependency; #547 Draft provenance does not approve a current validation receipt, and Generic Risk Policy publication cannot substitute for it.

The consumer currently projects one canonical candidate/position into the existing manual state. Mixed canonical candidates, partial canonical closes and legacy candle-triggered canonical settlement fail closed; there is no fabricated partial Full Cost completion. #1085 must implement and prove its source/admission/authority/actual-consumer route before its seven findings can close.

## Actual implementation and traces

EL-CG011: `manual-paper-canonical-contract.service.ts` checks all eight entry components and calls the existing trigger-bound validator, `adaptNaturalPaperSettlementFullCost`, frozen lifecycle consumer and `settleFourMarketPaperSample`. `paper-trading-position.service.ts` consumes that canonical settlement inside the real close operation; it does not promote legacy fee/slippage/funding bookkeeping.

EL-CG012: the same real close operation writes canonical NetPnL into fill, position, account/risk state and journal. Journal/fill/position carry the entire `canonicalPaper` payload: entry sample, entry component evidence, original validation receipt and owner verification, full eight-component exit evidence, binding, frozen trigger and canonical settlement. Existing JSON state/sync payload transport preserves it. No DB migration or DB mutation is performed.

EL-CG013: `consumeManualSameCandidateValidationReceipt` binds a digest-verified owner readback to the identical candidate. `prepareManualPaperCanonicalEvidence` calls it before the actual action and verifies persisted consumers. The actual route invokes the owner resolver with authenticated account identity; no configured source means fail closed.

| Identity | Source → manual request → order → position → fill/journal |
|---|---|
| candidateId | Natural position/sample exact frozen ID → `request.canonicalIdentity` → `canonicalPaper.identity` at every consumer |
| strategyId | Exact Natural sample/position/lifecycle strategy → same trace |
| parameterHash | Exact parameter hash; parameterDigest equality required → same trace |
| market | Source market, never inferred from a UI label → same trace |
| symbol | Source symbol equality; actual record symbol checked → same trace |
| timeframe | Natural sample primary timeframe, preserved as identity → same trace |
| side | Explicit execution direction; separate original signalDirection preserved → same trace |
| leverage | Futures public evidence/accounting equality; cash markets require explicit owner receipt identity → same trace |

`accountMode=PAPER`, parameter digest/hash, research SHA, sample ID, Natural position ID, original signalDirection and frozen lifecycle equality are retained. `BRIDGE_READY` or `sampleExecutionReady=false` cannot become execution proof.

| Cost | Entry → actual settlement → stored evidence |
|---|---|
| commission | Owner component/source/provenance/sample/position/identity/policy binding → canonical immediate cost → complete payload |
| tax | Same entry binding → existing adapter market applicability and trigger-bound component → complete payload |
| spread | Same entry binding → canonical fill model/exit evidence, without a second deduction → complete payload |
| slippage | Same entry binding → canonical adverse fill/exit evidence, without a second deduction → complete payload |
| funding | Same entry binding → observed holding-period funding validator for Futures → payments/component/binding retained |
| latency | Same entry binding → canonical fill model/trigger-bound evidence → complete payload |
| liquidityImpact | Same entry binding → canonical fill model/trigger-bound evidence → complete payload |
| partialFillImpact | Same entry binding → canonical fill model/trigger-bound evidence; full settlement required → complete payload |

NetPnL remains the existing canonical calculation: gross PnL from canonical fill prices minus entry immediate costs, exit immediate costs and validated funding. Costs already represented in fill prices are not subtracted again. Missing is never converted into zero. Manual actions receive zero Natural sample/economic credit.

Two discovered Paper contract defects are repaired without policy changes: the trigger-bound producer now accepts the exact existing `phase3-candidate:sha256:` namespace already accepted by recurring/lifecycle; the sampler retains explicit original `signalDirection` independently from execution direction.

## Validation and stop line

Regressions run through the real engine and actual authenticated HTTP route. Positive tests use controlled test-only owner-reader stubs and existing Natural/sampler fixtures; they neither publish a genuine receipt nor create runtime/economic credit. Negative cases include every identity mismatch, all eight missing components, stale/missing/unverified/synthetic/test-only/reference receipts, missing owner state readback, actual persisted side/leverage drift, missing/copied producer capability, bridge-not-ready and unsupported partial/candle settlement. Nonzero existing sampler fixture rates prove no double charging; Futures LONG/SHORT tests prove leverage/original-direction preservation.

No workflow, bounded allowlist, trigger, threshold, timeout, skip, release context, activation, server environment, Secret, DB or deployment changes. Fast CI is development evidence only. The existing Paper bounded-diff failure remains authoritative, including when it rejects these manual Paper paths. No Ready, Full CI dispatch, Merge, Staging, Production, schedule activation, private trading API or real order is authorized by this handoff.
