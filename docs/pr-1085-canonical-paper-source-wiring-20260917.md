# #1085 canonical Paper source wiring — Draft incomplete

This checkpoint supersedes the older assertions that `/scanner/plans` is absent and that Paper-owner acceptance is unconfirmed. Those documents remain historical. It does not supersede any safety gate or claim execution closure.

## Implemented product boundary

The existing stock and crypto scan routes capture only their actual returned server cards, bound to the authenticated account, exact run/signal reference and immutable code SHA. `ProductPaperSourceRegistry` keeps cloned, deep-frozen source snapshots, a 256-record bound and a 30-second freshness limit. Repeated references cannot refresh or overwrite snapshots. Saturation refuses new references rather than evicting still-live keys, so the same reference cannot be rehydrated with another snapshot. Missing/stale/cross-account/cross-SHA references never resolve to “latest”. This is a product source cache, not frozen research admission, a validation receipt or an execution authority.

`/api/trade-automation/scanner/plans` is now mounted through the existing trade-automation router without editing the #1042-owned route index. It requires the existing admin boundary and Paper-only approval envelope, an explicit eligible side and exact symbol/timeframe/source checks. It calls the existing canonical Scanner resolver; it never forwards to the generic trade plan endpoint. Client leverage and client evidence/receipts are rejected. Original signal direction is retained separately from the canonical entry direction. The current route deliberately returns HTTP 503 with `executionConnected=false`, no plan/order/position success and zero evidence credit because real admission/execution is not connected.

Successful Backtest runs capture their accepted server parameters and actual returned handoffs only after bounded execution completes without cancellation. A separate opaque run reference is transport metadata, not a new candidate namespace. `/api/backtests/paper/validate` compares every identity and policy reference against that account-bound immutable snapshot. URL identity is a claim, never execution authority. Missing timeframe no longer becomes 15m. Initial capital and search dates do not remint candidate identity; they remain accepted run metadata. This route also returns HTTP 503 after source verification: frozen research, genuine receipt and canonical strategy execution remain unproven.

Frontend handoffs carry actual server run/signal references and explicit side. Backtest source verification is clearly labelled “execution not performed”. Obsolete requests are aborted/isolated; the browser auth client loads only inside the verification event, leaving inert SSR references independent of browser environment. KR-only Composer support and the protective US blocker remain honest. Spot/Futures are not marked connected merely because buttons or a shared server resolver exist.

## Owner boundary and acceptance

The user directly assigned EL-CG011/012/013 to the existing task **Repair and activate paper risk flow**, task `01a0a969-2ab0-7971-b52b-39934162761f`, handoff turn `01a0ad6c-30fc-7922-8a70-93476cb9abe2`. That task accepted separate Draft implementation responsibility. Acknowledgement is `ACCEPTED_DRAFT_IN_PROGRESS`, responsibility only, not receipt/runtime/closure proof. Historical #1001/#891 and open validation #547 retain contract provenance. All three findings remain OPEN.

#1085 does not edit that owner's manual Paper types/engine/position/candle/core/routes, its new canonical contract service, Natural settlement producer or original-direction sampler fix. Owner contracts are not copied into an alternate product engine. Actual source-reader/genuine receipt and canonical execution integration remain dependencies. Fixtures and test reader stubs cannot fill those missing runtime authorities.

Before source/audit batches, all nineteen open PR heads and full owner file lists were refreshed; no head changed. The only existing whole-PR path overlap is #1041 `api-server/test.mjs`, where the existing two Hub smoke-test additions and #1085's earlier Phase5 registration are additive. Its verified hunk still passes `git apply --check`; this turn does not edit the file. #1042 index/UI/E2E, #547 validation and other owner paths remain untouched. Semantic Paper ownership is recorded separately from exact-path overlap.

## Verification and unresolved truth

Local typecheck: existing backend and frontend TypeScript compiler with the unchanged tsconfig, PASS. Initial source/handoff Phase5: 97/97 PASS. A new saturation regression then produced 97 PASS / 1 FAIL on the FIFO source cache; the actual source now refuses overflow without changing the 256-record/30-second limits. Modified-source Phase5: 98/98 PASS, zero skipped. The strengthened regression also requires a saturated store not to issue an unstored Backtest run reference. API smoke: 110/110 PASS. Phase12 including the real Scanner HTTP route: 314/314 PASS. Rendered-reference/exact-head checks: 11/11 PASS. Auditor regressions: 31/31 PASS, including a source-only route fixture that remains non-PROVEN and accepted owner responsibility that still leaves implementation gaps OPEN. No skipped test, assertion weakening, fake environment, dependency purge, timeout increase, rerun-to-pass or gate modification is used. Exact new-head Fast CI must be read after publication; old d20f45cf/e69ef0c8 CI is not inherited.

All seven #1085 findings remain ledger OPEN and implementation PARTIAL. Source comparison is now real, but the server route does not yet call canonical admission/simulation authority/recurring execution nor yield a same-candidate sample/position/lifecycle. Scanner leverage is not issued by its identity resolver; Backtest identity is not a frozen research candidate with a genuine receipt. These are actual implementation/authority dependencies, not reclassified safety-only or runtime-only findings.

Reconciled audit: P0 OPEN 0, P1 OPEN 10, #1085 REAL GAP 7, EXISTING OWNER 3. Combined graph 20 PROVEN / 9 PARTIAL / 1 MISSING / 0 UNKNOWN, critical identity 1/3 and Golden Journey 6/13, static evidence only. Applying the same auditor to current main `2b96802a86c9d19f4c8620b71252af2f9184633d` gives P0/P1 0/12, graph 18/11/1/0, identity 0/3, journey 4/13. Runtime/E2E/production and economic closure remain NOT_PROVEN.

The unchanged Paper bounded gate checks the **whole PR** diff against its existing non-UI allowed paths, not a narrowed Paper-only subdiff. Earlier Paper strategy/runtime paths trigger it; Product Integrity scripts and frontend wiring lie outside that contract. Its original workflow blob remains `e6a49f7cdb505120f2294d69500dd0cf642bac64`. Do not expand its allowlist, narrow its range, split/hide evidence to evade it, or treat a failure as success. Preserve authoritative failure until an owner-preserving, non-evasive dependency structure is genuinely established.

No Ready, canonical Full CI dispatch, Merge, Staging, Production, activation/schedule, DB/secret/environment mutation, private trading API, live trading or real financial action is performed. This checkpoint is incomplete. 현재 승인할 것 없음. Final exact HEAD/CI/status is reported separately after non-force publication; this file cannot self-certify its future commit SHA.
