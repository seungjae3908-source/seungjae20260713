{
  "name": "trade-execution-gateway",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "check": "node --check src/server.mjs && node --check src/public-websocket-runtime.mjs && node --check src/paper-state-store.mjs",
    "test": "npm run check && node --test tests/*.test.mjs"
  }
}
