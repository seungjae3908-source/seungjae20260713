import test from "node:test";
import assert from "node:assert/strict";
import {adoptExistingAuthoritativePaperRuntimeStageEvidenceV1 as adopt,callAuthoritativePaperRuntimeWithStageEvidenceV1 as call,reconcileAuthoritativePaperRuntimeStageEvidenceV1 as reconcile,reconcileSingleAuthoritativePaperRuntimeCandidateStageEvidenceV1 as single} from "../src/authoritative-paper-runtime-stage-evidence-caller-v1.js";

const SHA="c".repeat(40),D="b".repeat(64),CID=`phase3-candidate:sha256:${"a".repeat(64)}`;
const E=Object.freeze({candidateId:CID,strategyFamily:"trend",strategyId:"s1",strategyVersion:"v1",parameterHash:D,parameterDigest:D,researchCodeSha:SHA,costPolicyVersion:"cost-v1",executionPolicyVersion:"exec-v1",market:"CRYPTO_FUTURES",provider:"bitget",symbol:"BTCUSDT",timeframe:"15m",sidePolicy:"LONG",accountMode:"PAPER"});
const safety=()=>({executionAuthority:"NONE",simulatedOnly:true,liveOrderAllowed:false,privateTradingApiAllowed:false,orderSubmitted:false,exchangeRequestSent:false,productionMutationAllowed:false});
function candidate({strategy={},signal={},provider=E.provider}={}){const id={candidateId:E.candidateId,strategyFamily:E.strategyFamily,strategyId:E.strategyId,strategyVersion:E.strategyVersion,parameterHash:E.parameterHash,parameterDigest:E.parameterDigest,researchCodeSha:E.researchCodeSha,costPolicyVersion:E.costPolicyVersion,executionPolicyVersion:E.executionPolicyVersion,accountMode:E.accountMode,...strategy};return Object.freeze({signal:Object.freeze({signalId:"sig1",market:E.market,symbol:E.symbol,timeframe:E.timeframe,direction:E.sidePolicy,signalDirection:E.sidePolicy,strategyIdentity:Object.freeze(id),...signal}),execution:Object.freeze({dataEvidence:Object.freeze({provider})}),paperIdentity:Object.freeze({candidateId:id.candidateId,strategyFamily:id.strategyFamily,strategyId:id.strategyId,strategyVersion:id.strategyVersion,parameterHash:id.parameterHash,parameterDigest:id.parameterDigest,researchCodeSha:id.researchCodeSha,costPolicyVersion:id.costPolicyVersion,market:signal.market??E.market,symbol:signal.symbol??E.symbol,timeframe:signal.timeframe??E.timeframe,direction:signal.direction??E.sidePolicy,accountMode:id.accountMode}),...safety()});}
const st=(name,status,count,blocker=null)=>Object.freeze({stage:name,status,count,blocker,provenance:status==="MEASURED"?`t:${name}`:null,measuredAtMs:1789000000000});
function factory(cands=[candidate()]){return Object.freeze({status:"PAPER_CANDIDATES_READY",paperBridge:Object.freeze({candidates:Object.freeze(cands),exitSignals:Object.freeze([])}),stageMeasurements:Object.freeze([st("Scanner Candidate","MEASURED",1),st("Profit Gate","MEASURED",1),st("Identity","MEASURED",1),st("Paper Admission","MEASURED",1),st("Entry","UNKNOWN",null,"ENTRY_UNKNOWN"),st("Position","UNKNOWN",null,"POSITION_UNKNOWN"),st("Exit","MEASURED",1),st("Settlement","UNKNOWN",null,"SETTLEMENT_UNKNOWN")]),entryCount:null,settlementCount:null,...safety(),profitabilityClaimAllowed:false});}
const ev=(field,ids)=>Object.freeze({field,status:"MEASURED",count:ids.length,blocker:null,provenance:`loop:${field}`,observedAt:1789000000100,observationIds:Object.freeze([...ids]),naturalCredit:0,replayCredit:0,duplicateCredit:0});
const sample=(id="e1",x={})=>Object.freeze({paperSampleId:id,identity:Object.freeze({candidateId:E.candidateId,strategyFamily:E.strategyFamily,strategyId:E.strategyId,strategyVersion:E.strategyVersion,parameterHash:E.parameterHash,parameterDigest:E.parameterDigest,researchCodeSha:E.researchCodeSha,accountMode:E.accountMode,market:E.market,symbol:E.symbol,timeframe:E.timeframe,executionDirection:E.sidePolicy,...x}),profitEvidence:Object.freeze({costPolicyId:E.costPolicyVersion}),entryEvidenceProvenance:Object.freeze({provider:E.provider})});
const pos=(id="p1",x={})=>Object.freeze({positionId:id,candidateId:E.candidateId,strategyFamily:E.strategyFamily,strategyId:E.strategyId,strategyVersion:E.strategyVersion,parameterHash:E.parameterHash,parameterDigest:E.parameterDigest,researchCodeSha:E.researchCodeSha,accountMode:E.accountMode,costPolicyVersion:E.costPolicyVersion,market:E.market,symbol:E.symbol,direction:E.sidePolicy,sample:sample(),...x});
const set=(id="z1",x={})=>Object.freeze({settlementId:id,candidateId:E.candidateId,strategyFamily:E.strategyFamily,strategyId:E.strategyId,strategyVersion:E.strategyVersion,parameterHash:E.parameterHash,parameterDigest:E.parameterDigest,researchCodeSha:E.researchCodeSha,accountMode:E.accountMode,costPolicyVersion:E.costPolicyVersion,market:E.market,symbol:E.symbol,timeframe:E.timeframe,entryDirection:E.sidePolicy,entryEvidenceProvenance:Object.freeze({provider:E.provider}),exitEvidenceProvenance:Object.freeze({provider:E.provider}),...x});
function recurring({entry=["e1"],positionsIds=["p1"],settle=["z1"],samples=[sample()],positions=[pos()],settlements=[set()]}={}){return Object.freeze({state:Object.freeze({identity:Object.freeze({candidateId:E.candidateId,strategyFamily:E.strategyFamily,strategyId:E.strategyId,strategyVersion:E.strategyVersion,parameterHash:E.parameterHash,parameterDigest:E.parameterDigest,researchCodeSha:E.researchCodeSha,costPolicyVersion:E.costPolicyVersion,executionPolicyVersion:E.executionPolicyVersion,accountMode:E.accountMode}),samples:Object.freeze(samples),positions:Object.freeze(positions),settlements:Object.freeze(settlements)}),summary:Object.freeze({canonicalNaturalStageEvidence:Object.freeze({schemaVersion:"canonical-natural-paper-loop-stage-evidence-v1",identity:Object.freeze({strategySha:SHA,runtimeSha:SHA}),stageCounts:Object.freeze({entry:ev("entry",entry),position:ev("position",positionsIds),settlement:ev("settlement",settle)}),naturalCredit:0,replayCredit:0,duplicateCredit:0,replayed:false})})});}

test("exact full identity reconcile PASS and zero credit",()=>{const r=reconcile({paperRuntimeResult:factory(),recurringCycleResult:recurring(),expectedCandidateIdentity:E});assert.equal(r.stageEvidenceConnection.status,"RECONCILED");assert.equal(r.entryCount,1);assert.equal(r.settlementCount,1);assert.equal(r.sampleCredit,0);assert.equal(r.executionRealismCredit,0);assert.equal(r.profitabilityCredit,0);});

test("single helper derives canonical full identity",()=>{const r=single({paperRuntimeResult:factory(),recurringCycleResult:recurring()});assert.equal(r.recurringStageEvidence.candidateIdentity.executionPolicyVersion,E.executionPolicyVersion);for(const k of ["market","provider","symbol","timeframe","sidePolicy"])assert.equal(r.recurringStageEvidence.candidateIdentity[k],E[k]);});

test("02-16 factory candidate mismatch BLOCK",()=>{const cases=[["candidateId",{strategy:{candidateId:`phase3-candidate:sha256:${"d".repeat(64)}`}}],["strategyId",{strategy:{strategyId:"x"}}],["strategyFamily",{strategy:{strategyFamily:"x"}}],["strategyVersion",{strategy:{strategyVersion:"x"}}],["parameterDigest",{strategy:{parameterDigest:"d".repeat(64)}}],["parameterHash",{strategy:{parameterHash:"d".repeat(64)}}],["researchCodeSha",{strategy:{researchCodeSha:"d".repeat(40)}}],["costPolicyVersion",{strategy:{costPolicyVersion:"x"}}],["executionPolicyVersion",{strategy:{executionPolicyVersion:"x"}}],["market",{signal:{market:"CRYPTO_SPOT"}}],["provider",{provider:"other"}],["symbol",{signal:{symbol:"ETHUSDT"}}],["timeframe",{signal:{timeframe:"1h"}}],["side",{signal:{direction:"SHORT",signalDirection:"SHORT"}}],["accountMode",{strategy:{accountMode:"LIVE"}}]];for(const [name,patch] of cases)assert.throws(()=>reconcile({paperRuntimeResult:factory([candidate(patch)]),recurringCycleResult:recurring(),expectedCandidateIdentity:E}),/PAPER_STAGE_FACTORY_CANDIDATE_BINDING_MISSING/u,name);});

test("17 missing identity BLOCK/UNKNOWN, 18 ambiguous candidates BLOCK",()=>{let r=adopt({paperRuntimeResult:factory([candidate({strategy:{executionPolicyVersion:null}})]),recurringCycleResult:recurring()});assert.equal(r.stageEvidenceConnection.status,"BLOCKED");assert.equal(r.entryCount,null);r=adopt({paperRuntimeResult:factory([candidate(),candidate()]),recurringCycleResult:recurring()});assert.equal(r.stageEvidenceConnection.status,"BLOCKED");assert.equal(r.entryCount,null);});

test("19 duplicate lifecycle and 20-22 cross-candidate lifecycle contamination BLOCK",()=>{let r=adopt({paperRuntimeResult:factory(),recurringCycleResult:recurring({samples:[sample(),sample()]})});assert.equal(r.stageEvidenceConnection.status,"BLOCKED");const bad=`phase3-candidate:sha256:${"f".repeat(64)}`;for(const patch of [{samples:[sample("e1",{candidateId:bad})]},{positions:[pos("p1",{candidateId:bad})]},{settlements:[set("z1",{candidateId:bad})]}]){r=adopt({paperRuntimeResult:factory(),recurringCycleResult:recurring(patch)});assert.equal(r.stageEvidenceConnection.status,"BLOCKED");}});

test("23 measured aggregate zero stays UNKNOWN",()=>{const r=reconcile({paperRuntimeResult:factory(),recurringCycleResult:recurring({entry:[],positionsIds:[],settle:[],samples:[],positions:[],settlements:[]}),expectedCandidateIdentity:E});assert.equal(r.entryCount,null);assert.equal(r.settlementCount,null);assert.equal(r.stageMeasurements.find(x=>x.stage==="Entry").status,"UNKNOWN");});

test("24 missing cost policy BLOCK not zero",()=>{const r=adopt({paperRuntimeResult:factory([candidate({strategy:{costPolicyVersion:null}})]),recurringCycleResult:recurring()});assert.equal(r.stageEvidenceConnection.status,"BLOCKED");assert.equal(r.stageEvidenceConnection.fullCostReady,false);assert.equal(r.entryCount,null);});

test("25 caller failure preserves original UNKNOWN and no credit",async()=>{const original=factory([candidate({signal:{market:"CRYPTO_SPOT"}})]);const r=await call({paperRuntimeForMarket:async()=>original,runtimeInput:{market:E.market},recurringCycleResult:recurring(),expectedCandidateIdentity:E});assert.equal(r.stageEvidenceConnection.status,"BLOCKED");assert.equal(r.stageMeasurements.find(x=>x.stage==="Entry").status,"UNKNOWN");assert.equal(r.entryCount,null);assert.equal(r.stageEvidenceConnection.sampleCredit,0);assert.equal(r.stageEvidenceConnection.profitabilityCredit,0);});

test("NO INPUT MUTATION",()=>{const p=structuredClone(factory()),r=structuredClone(recurring()),e=structuredClone(E),before=structuredClone({p,r,e});reconcile({paperRuntimeResult:p,recurringCycleResult:r,expectedCandidateIdentity:e});assert.deepEqual({p,r,e},before);});
