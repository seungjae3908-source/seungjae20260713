name: ROOM B 780 Partial Frame Probe

on:
  push:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-24.04
    steps:
      - name: Verify workflow registration
        run: echo "ROOM_B_780_WORKFLOW_REGISTERED"
