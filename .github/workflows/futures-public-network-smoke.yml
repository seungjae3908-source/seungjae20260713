name: Application CI

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
  push:
    branches:
      - main
    paths:
      - .github/workflows/futures-public-network-smoke.yml
      - pnpm-lock.yaml
      - pnpm-workspace.yaml
      - package.json
      - api-server/**
      - stock-analyzer/**
      - packages/**

permissions:
  actions: read
  contents: read
  statuses: write

concurrency:
  group: application-ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Typecheck, unit, smoke, and build
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Publish pending Application CI status
        uses: actions/github-script@v7
        with:
          script: |
            const sha = context.payload.pull_request?.head?.sha ?? context.sha;
            await github.rest.repos.createCommitStatus({ owner: context.repo.owner, repo: context.repo.repo, sha, state: 'pending', target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`, description: `Application CI run ${context.runId}: running`, context: 'application-ci/verified' });
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up pnpm 9
        uses: pnpm/action-setup@v4
        with:
          version: 9
          run_install: false
      - name: Set up Node 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
      - name: Install workspace dependencies with frozen lockfile
        run: pnpm install --frozen-lockfile
      - name: Typecheck frontend
        run: pnpm --dir stock-analyzer run typecheck
      - name: Typecheck backend
        run: pnpm --dir stock-analyzer exec tsc -p ../api-server/tsconfig.json --noEmit
      - name: Run Phase 2 futures tests
        run: pnpm --dir api-server run test:phase2
      - name: Run Phase 3 trading risk tests
        run: pnpm --dir api-server run test:risk
      - name: Run Phase 4 contract rules and risk regression tests
        run: pnpm --dir api-server run test:phase4
      - name: Run Phase 5 backtest tests
        run: pnpm --dir api-server run test:phase5
      - name: Run Phase 6 paper trading tests
        run: pnpm --dir api-server run test:phase6
      - name: Run Phase 7 journal sync and analytics tests
        run: pnpm --dir api-server run test:phase7
      - name: Run Phase 8 release candidate tests
        run: pnpm --dir api-server run test:phase8
      - name: Run Phase 9 privacy-safe AI review tests
        run: pnpm --dir api-server run test:phase9
      - name: Run trade automation safety and adapter tests
        run: pnpm --dir api-server run test:phase12
      - name: Run API smoke tests
        run: pnpm --dir api-server run test:smoke
      - name: Build frontend production bundle
        run: pnpm --dir stock-analyzer run build
      - name: Build backend production bundle
        run: pnpm --dir api-server run build:server

  browser-ui:
    name: Playwright desktop and mobile application UI
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Publish pending browser UI status
        uses: actions/github-script@v7
        with:
          script: |
            const sha = context.payload.pull_request?.head?.sha ?? context.sha;
            await github.rest.repos.createCommitStatus({ owner: context.repo.owner, repo: context.repo.repo, sha, state: 'pending', target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`, description: `Browser UI run ${context.runId}: running`, context: 'browser-ui/verified' });
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up pnpm 9
        uses: pnpm/action-setup@v4
        with:
          version: 9
          run_install: false
      - name: Set up Node 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
      - name: Install workspace dependencies with frozen lockfile
        run: pnpm install --frozen-lockfile
      - name: Install Playwright Chromium
        run: pnpm --dir stock-analyzer exec playwright install --with-deps chromium
      - name: Run Playwright desktop and mobile tests
        id: browser
        run: pnpm --dir stock-analyzer run test:e2e
      - name: Upload Playwright report and failure evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phase9-playwright-${{ github.run_id }}
          path: |
            stock-analyzer/test-results
            stock-analyzer/playwright-report
          if-no-files-found: warn
          retention-days: 7
      - name: Publish browser UI result
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const result = '${{ steps.browser.outcome }}' || 'failure';
            const sha = context.payload.pull_request?.head?.sha ?? context.sha;
            const current = await github.rest.actions.getWorkflowRun({ owner: context.repo.owner, repo: context.repo.repo, run_id: context.runId });
            const runs = await github.paginate(github.rest.actions.listWorkflowRuns, { owner: context.repo.owner, repo: context.repo.repo, workflow_id: current.data.workflow_id, head_sha: sha, per_page: 100 });
            const superseded = runs.some((run) => run.id > context.runId && run.head_sha === sha && ['queued', 'in_progress', 'completed', 'waiting', 'pending', 'requested'].includes(run.status));
            if (superseded) {
              core.notice('Newer workflow run exists for this HEAD; skipping stale browser status update.');
              return;
            }
            const state = result === 'success' ? 'success' : result === 'cancelled' ? 'error' : 'failure';
            await github.rest.repos.createCommitStatus({ owner: context.repo.owner, repo: context.repo.repo, sha, state, target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`, description: `Browser UI run ${context.runId}: ${result}`, context: 'browser-ui/verified' });

  database-rls:
    name: Disposable PostgreSQL migration and RLS integration
    runs-on: ubuntu-latest
    timeout-minutes: 15
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: phase8
          POSTGRES_PASSWORD: phase8-ci-only
          POSTGRES_DB: phase8
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U phase8 -d phase8"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      PGHOST: 127.0.0.1
      PGPORT: 5432
      PGUSER: phase8
      PGPASSWORD: phase8-ci-only
      PGDATABASE: phase8
    steps:
      - name: Publish pending database RLS status
        uses: actions/github-script@v7
        with:
          script: |
            const sha = context.payload.pull_request?.head?.sha ?? context.sha;
            await github.rest.repos.createCommitStatus({ owner: context.repo.owner, repo: context.repo.repo, sha, state: 'pending', target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`, description: `Database RLS run ${context.runId}: running`, context: 'database-rls/verified' });
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Install PostgreSQL client
        run: sudo apt-get update -qq && sudo apt-get install -y postgresql-client
      - name: Apply, test, rollback, and reapply migrations
        id: database
        run: bash api-server/scripts/verify-phase8-db.sh
      - name: Publish database RLS result
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const result = '${{ steps.database.outcome }}' || 'failure';
            const sha = context.payload.pull_request?.head?.sha ?? context.sha;
            const current = await github.rest.actions.getWorkflowRun({ owner: context.repo.owner, repo: context.repo.repo, run_id: context.runId });
            const runs = await github.paginate(github.rest.actions.listWorkflowRuns, { owner: context.repo.owner, repo: context.repo.repo, workflow_id: current.data.workflow_id, head_sha: sha, per_page: 100 });
            const superseded = runs.some((run) => run.id > context.runId && run.head_sha === sha && ['queued', 'in_progress', 'completed', 'waiting', 'pending', 'requested'].includes(run.status));
            if (superseded) {
              core.notice('Newer workflow run exists for this HEAD; skipping stale database status update.');
              return;
            }
            const state = result === 'success' ? 'success' : result === 'cancelled' ? 'error' : 'failure';
            await github.rest.repos.createCommitStatus({ owner: context.repo.owner, repo: context.repo.repo, sha, state, target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`, description: `Database RLS run ${context.runId}: ${result}`, context: 'database-rls/verified' });

  security-integration:
    name: Security input, bundle, and outbound safety verification
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Publish pending security status
        uses: actions/github-script@v7
        with:
          script: |
            const sha = context.payload.pull_request?.head?.sha ?? context.sha;
            await github.rest.repos.createCommitStatus({ owner: context.repo.owner, repo: context.repo.repo, sha, state: 'pending', target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`, description: `Security integration run ${context.runId}: running`, context: 'security-integration/verified' });
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up pnpm 9
        uses: pnpm/action-setup@v4
        with:
          version: 9
          run_install: false
      - name: Set up Node 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
      - name: Install workspace dependencies with frozen lockfile
        run: pnpm install --frozen-lockfile
      - name: Run Phase 8 security and load tests
        run: pnpm --dir api-server run test:phase8
      - name: Build frontend for bundle inspection
        run: pnpm --dir stock-analyzer run build
      - name: Scan source and production bundle
        id: security
        working-directory: api-server
        run: node ./scripts/verify-phase8-security.mjs
      - name: Publish security result
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const result = '${{ steps.security.outcome }}' || 'failure';
            const sha = context.payload.pull_request?.head?.sha ?? context.sha;
            const current = await github.rest.actions.getWorkflowRun({ owner: context.repo.owner, repo: context.repo.repo, run_id: context.runId });
            const runs = await github.paginate(github.rest.actions.listWorkflowRuns, { owner: context.repo.owner, repo: context.repo.repo, workflow_id: current.data.workflow_id, head_sha: sha, per_page: 100 });
            const superseded = runs.some((run) => run.id > context.runId && run.head_sha === sha && ['queued', 'in_progress', 'completed', 'waiting', 'pending', 'requested'].includes(run.status));
            if (superseded) {
              core.notice('Newer workflow run exists for this HEAD; skipping stale security status update.');
              return;
            }
            const state = result === 'success' ? 'success' : result === 'cancelled' ? 'error' : 'failure';
            await github.rest.repos.createCommitStatus({ owner: context.repo.owner, repo: context.repo.repo, sha, state, target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`, description: `Security integration run ${context.runId}: ${result}`, context: 'security-integration/verified' });

  ai-privacy:
    name: AI privacy, prompt, output, and outbound verification
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Publish pending AI privacy status
        uses: actions/github-script@v7
        with:
          script: |
            const sha = context.payload.pull_request?.head?.sha ?? context.sha;
            await github.rest.repos.createCommitStatus({ owner: context.repo.owner, repo: context.repo.repo, sha, state: 'pending', target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`, description: `AI privacy run ${context.runId}: running`, context: 'ai-privacy/verified' });
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with: { version: 9, run_install: false }
      - uses: actions/setup-node@v4
        with: { node-version: "20", cache: pnpm, cache-dependency-path: pnpm-lock.yaml }
      - run: pnpm install --frozen-lockfile
      - name: Run guarded mock provider and zero-outbound privacy tests
        run: pnpm --dir api-server run test:phase9
      - name: Build frontend for secret inspection
        run: pnpm --dir stock-analyzer run build
      - name: Verify AI privacy and zero outbound contract
        id: ai
        working-directory: api-server
        run: node ./scripts/verify-phase9-ai-privacy.mjs
      - name: Publish AI privacy result
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const result = '${{ steps.ai.outcome }}' || 'failure';
            const sha = context.payload.pull_request?.head?.sha ?? context.sha;
            const current = await github.rest.actions.getWorkflowRun({ owner: context.repo.owner, repo: context.repo.repo, run_id: context.runId });
            const runs = await github.paginate(github.rest.actions.listWorkflowRuns, { owner: context.repo.owner, repo: context.repo.repo, workflow_id: current.data.workflow_id, head_sha: sha, per_page: 100 });
            if (runs.some((run) => run.id > context.runId && run.head_sha === sha && ['queued','in_progress','completed','waiting','pending','requested'].includes(run.status))) return;
            const state = result === 'success' ? 'success' : result === 'cancelled' ? 'error' : 'failure';
            await github.rest.repos.createCommitStatus({ owner: context.repo.owner, repo: context.repo.repo, sha, state, target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`, description: `AI privacy run ${context.y������k�w��E 저장 실패: ${cause.message}`
					: "서버 백업 저장에 실패했습니다.",
			);
		} finally {
			setRemoteBackupBusy(false);
		}
	};

	const restoreServerBackup = async () => {
		if (!memberId) {
			setBackupStatus(
				"승인된 계정으로 로그인해야 서버 백업을 사용할 수 있습니다.",
			);
			return;
		}

		const confirmed = window.confirm(
			"서버에 저장된 최신 설정으로 현재 기기 설정을 바꿉니다. 계속할까요?",
		);

		if (!confirmed) return;

		setRemoteBackupBusy(true);

		try {
			const count = await restoreRemoteBackup(memberId);
			setBackupStatus(
				`서버 백업 ${count}개 항목을 복원했습니다. 화면을 새로고침합니다.`,
			);

			window.setTimeout(() => {
				window.location.reload();
			}, 300);
		} catch (cause) {
			setBackupStatus(
				cause instanceof Error
					? `서버 백업 복원 실패: ${cause.message}`
					: "서버 백업 복원에 실패했습니다.",
			);
			setRemoteBackupBusy(false);
		}
	};

	const exportBackup = () => {
		const backup = {
			app: "seungjae-stock",
			version: 1,
			exportedAt: new Date().toISOString(),
			localStorage: readBackupData(),
		};

		const blob = new Blob([JSON.stringify(backup, null, 2)], {
			type: "application/json",
		});

		const url = window.URL.createObjectURL(blob);
		const anchor = document.createElement("a");

		anchor.href = url;
		anchor.download = `seungjae-stock-backup-${new Date()
			.toISOString()
			.slice(0, 10)}.json`;
		anchor.click();

		window.URL.revokeObjectURL(url);
		setBackupStatus("백업 파일을 저장했습니다.");
	};

	const importBackup = async (event: ChangeEvent<HTMLInputElement>) => {
		const file = event.target.files?.[0];

		if (!file) return;

		try {
			const text = await file.text();
			const parsed = JSON.parse(text) as {
				localStorage?: Record<string, string>;
			};

			if (!parsed.localStorage || typeof parsed.localStorage !== "object") {
				setBackupStatus("백업 파일 형식이 맞지 않습니다.");
				return;
			}

			Object.entries(parsed.localStorage).forEach(([key, value]) => {
				window.localStorage.setItem(key, value);
			});

			setBackupStatus("백업을 복원했습니다. 새로고침하면 반영됩니다.");
		} catch {
			setBackupStatus("백업 복원에 실패했습니다.");
		} finally {
			event.target.value = "";
		}
	};

	return (
		<div className="flex h-full min-h-0 flex-col overflow-hidden bg-background text-center">
			<header className="border-b border-card-border bg-background/90 px-4 pb-4 pt-5 glass">
				<h1 className="text-xl font-extrabold">설정</h1>
			</header>

			<main className="min-h-0 flex-1 space-y-4 overflow-y-auto overscroll-contain p-4 pb-24">
				<TradeAutomationSettings />
				<section className="rounded-3xl border border-card-border bg-card p-4 shadow-sm">
					<h2 className="mb-3 text-sm font-extrabold">계정 · 자산</h2>

					<button
						type="button"
						onClick={() => navigate("/account")}
						className="flex w-full items-center gap-3 border-b border-card-border py-3 text-left"
					>
						<span className="flex h-9 w-9 items-center justify-center rounded-xl bg-secondary text-primary">
							<UserRound className="h-4 w-4" />
						</span>
						<div className="min-w-0 flex-1">
							<p className="text-sm font-extrabold">로그인 · 회원가입</p>
							<p className="mt-0.5 text-xs text-muted-foreground">
								관심종목, 알림, 포트폴리오를 계정에 저장합니다.
							</p>
						</div>
					</button>

					<button
						type="button"
						onClick={() => navigate("/portfolio")}
						className="flex w-full items-center gap-3 py-3 text-left"
					>
						<span className="flex h-9 w-9 items-center justify-center rounded-xl bg-secondary text-primary">
							<WalletCards className="h-4 w-4" />
						</span>
						<div className="min-w-0 flex-1">
							<p className="text-sm font-extrabold">내 포트폴리오</p>
							<p className="mt-0.5 text-xs text-muted-foreground">
								매수가, 수량, 평가손익과 수익률을 확인합니다.
							</p>
						</div>
					</button>
				</section>

				<section className="rounded-3xl border border-card-border bg-card p-4 shadow-sm">
					<h2 className="mb-3 text-sm font-extrabold">화면 설정</h2>

					<button
						type="button"
						onClick={toggleTheme}
						className="flex w-full items-center gap-3 border-b border-card-border py-3 text-left last:border-b-0"
					>
						<span className="flex h-9 w-9 items-center justify-center rounded-xl bg-secondary text-primary">
							{isDark ? (
								<Moon className="h-4 w-4" />
							) : (
								<Sun className="h-4 w-4" />
							)}
						</span>

						<div className="min-w-0 flex-1">
							<p className="break-keep text-sm font-extrabold leading-relaxed">
								다크모드
							</p>

							<p className="mt-0.5 break-keep text-xs leading-relaxed text-muted-foreground">
								{isDark ? "어두운 화면 사용 중" : "밝은 화면 사용 중"}
							</p>
						</div>

						<span
							className={cn(
								"h-5 w-9 shrink-0 rounded-full p-0.5 transition-colors",
								isDark ? "bg-primary" : "bg-muted",
							)}
						>
							<span
								className={cn(
									"block h-4 w-4 rounded-full bg-background transition-transform",
									isDark && "translate-x-4",
								)}
							/>
						</span>
					</button>

					<div className="py-3 last:border-b-0">
						<div className="mb-3 flex items-center gap-3 text-left">
							<span className="flex h-9 w-9 items-center justify-center rounded-xl bg-secondary text-primary">
								<Palette className="h-4 w-4" />
							</span>

							<div className="min-w-0 flex-1">
								<p className="text-sm font-extrabold">강조 색상</p>

								<p className="mt-0.5 break-keep text-xs leading-relaxed text-muted-foreground">
									앱의 포인트 색상을 즉시 바꿉니다.
								</p>
							</div>
						</div>

						<div className="grid grid-cols-3 gap-2">
							{ACCENT_COLORS.map((color) => (
								<button
									key={color.key}
									type="button"
									onClick={() => changeAccentColor(color.key)}
									className={cn(
										"flex items-center justify-center gap-1.5 rounded-2xl border px-2 py-2 text-xs font-extrabold",
										accentColor === color.key
											? "border-primary bg-primary/10 text-primary"
											: "border-card-border bg-background text-muted-foreground",
									)}
								>
									<span
										className={cn("h-3 w-3 rounded-full", color.className)}
									/>
									{color.label}
								</button>
							))}
						</div>
					</div>
				</section>

				<section className="rounded-3xl border border-card-border bg-card p-4 shadow-sm">
					<div className="mb-3">
						<h2 className="text-sm font-extrabold">휴대폰 알림</h2>

						<p className="mt-1 break-keep text-xs leading-relaxed text-muted-foreground">
							관심종목 뉴스, 공시, 등락률, 목표가/손절가 접근 알림을 받을 수
							있게 연결합니다.
						</p>
					</div>

					<div className="grid grid-cols-1 gap-2">
						<button
							type="button"
							onClick={() => void requestPermission()}
							className="flex w-full items-center justify-center gap-2 rounded-2xl border border-primary/40 bg-primary/10 p-3 text-sm font-extrabold text-primary"
						>
							<Smartphone className="h-4 w-4" />
							브라우저 알림 권한 켜기
						</button>

						<button
							type="button"
							onClick={() => void enablePush()}
							disabled={!pushSupported}
							className={cn(
								"flex w-full items-center justify-center gap-2 rounded-2xl border p-3 text-sm font-extrabold",
								pushSupported
									? "border-positive/40 bg-positive/10 text-positive"
									: "border-card-border bg-background text-muted-foreground opacity-60",
							)}
						>
							<Bell className="h-4 w-4" />
							푸시 알림 구독하기
						</button>
					</div>

					{!hasVapidKey && (
						<p className="mt-2 break-keep text-xs font-bold leading-relaxed text-warning">
							서버 푸시 키 설정 필요 — 브라우저 알림 권한은 지금도 사용할 수
							있습니다.
						</p>
					)}

					<p className="mt-2 break-keep text-xs font-bold leading-relaxed text-muted-foreground">
						현재 상태: {noticeStatus}
					</p>
				</section>

				<section className="rounded-3xl border border-card-border bg-card p-4 shadow-sm">
					<div className="mb-3">
						<h2 className="text-sm font-extrabold">관심종목 알림 종류</h2>

						<p className="mt-1 break-keep text-xs leading-relaxed text-muted-foreground">
							필요한 알림만 켜서 관리합니다.
						</p>
					</div>

					<div className="space-y-2">
						{WATCHLIST_ALERT_TYPES.map((item) => {
							const active = enabledAlerts.includes(item.key);

							return (
								<button
									key={item.key}
									type="button"
									onClick={() => toggleAlert(item.key)}
									className={cn(
										"flex w-full items-center gap-3 rounded-2xl border p-3 text-left transition-colors",
										active
											? "border-primary/40 bg-primary/10"
											: "border-card-border bg-background",
									)}
								>
									<span
										className={cn(
											"flex h-9 w-9 shrink-0 items-center justify-center rounded-xl",
											active
												? "bg-primary text-primary-foreground"
												: "bg-secondary text-muted-foreground",
										)}
									>
										{ALERT_ICONS[item.key]}
									</span>

									<span className="min-w-0 flex-1">
										<span className="block break-keep text-sm font-extrabold leading-relaxed">
											{item.title}
										</span>

										<span className="mt-0.5 block break-keep text-xs leading-relaxed text-muted-foreground">
											{item.desc}
										</span>
									</span>

									<span
										className={cn(
											"h-5 w-9 shrink-0 rounded-full p-0.5 transition-colors",
											active ? "bg-primary" : "bg-muted",
										)}
									>
										<span
											className={cn(
												"block h-4 w-4 rounded-full bg-background transition-transform",
												active && "translate-x-4",
											)}
										/>
									</span>
								</button>
							);
						})}
					</div>
				</section>

				<section className="rounded-3xl border border-card-border bg-card p-4 shadow-sm">
					<div className="mb-3">
						<h2 className="text-sm font-extrabold">서버 자동백업 / 복원</h2>

						<p className="mt-1 break-keep text-xs leading-relaxed text-muted-foreground">
							승인된 회원 계정에 화면, 검색기, 자동매매, 차트 설정을 최신 1개로
							저장합니다.
						</p>
					</div>

					<div className="grid grid-cols-2 gap-2">
						<button
							type="button"
							onClick={() => void saveServerBackup()}
							disabled={!memberId || remoteBackupBusy}
							className="flex items-center justify-center gap-1.5 rounded-2xl border border-primary/40 bg-primary/10 px-3 py-3 text-sm font-extrabold text-primary disabled:cursor-not-allowed disabled:opacity-50"
						>
							<Download className="h-4 w-4" />
							서버에 저장
						</button>

						<button
							type="button"
							onClick={() => void restoreServerBackup()}
							disabled={!memberId || remoteBackupBusy}
							className="flex items-center justify-center gap-1.5 rounded-2xl border border-positive/40 bg-positive/10 px-3 py-3 text-sm font-extrabold text-positive disabled:cursor-not-allowed disabled:opacity-50"
						>
							<Upload className="h-4 w-4" />
							서버 백업 복원
						</button>
					</div>

					<p className="mt-2 break-keep text-xs font-bold leading-relaxed text-muted-foreground">
						자동백업 상태: {remoteBackupStatus.message}
					</p>
				</section>

				<section className="rounded-3xl border border-card-border bg-card p-4 shadow-sm">
					<div className="mb-3">
						<h2 className="text-sm font-extrabold">파일 백업 / 복원</h2>

						<p className="mt-1 break-keep text-xs leading-relaxed text-muted-foreground">
							설정 데이터를 JSON 파일로 내려받거나 저장된 파일을 다시
							불러옵니다.
						</p>
					</div>

					<div className="grid grid-cols-2 gap-2">
						<button
							type="button"
							onClick={exportBackup}
							className="flex items-center justify-center gap-1.5 rounded-2xl border border-primary/40 bg-primary/10 px-3 py-3 text-sm font-extrabold text-primary"
						>
							<Download className="h-4 w-4" />
							파일 저장
						</button>

						<button
							type="button"
							onClick={() => inputRef.current?.click()}
							className="flex items-center justify-center gap-1.5 rounded-2xl border border-card-border bg-background px-3 py-3 text-sm font-extrabold"
						>
							<Upload className="h-4 w-4" />
							파일 불러오기
						</button>
					</div>

					<input
						ref={inputRef}
						type="file"
						accept="application/json"
						onChange={(event) => void importBackup(event)}
						className="hidden"
					/>

					<p className="mt-2 break-keep text-xs font-bold leading-relaxed text-muted-foreground">
						{backupStatus}
					</p>
				</section>

				<p className="pb-4 pt-2 text-center text-xs font-bold text-muted-foreground">
					By. Seung Jae Lee
				</p>
			</main>

			<BottomNav />
		</div>
	);
}
