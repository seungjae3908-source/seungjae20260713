# Stock App Codex Safety Rules

이 저장소는 실제 운영 중인 주식·코인 분석 앱의 소스 저장소다.

## 절대 금지
- 실제 매수·매도·정정·취소 API를 호출하거나 연결하지 않는다.
- 키움·비트겟·기타 거래소의 실주문 기능을 만들거나 활성화하지 않는다.
- 자동매매를 만들거나 활성화하지 않는다.
- `KIWOOM_MODE` 및 실주문 관련 환경변수를 변경하지 않는다.
- API 키, 토큰, 비밀번호, 쿠키, 인증정보를 출력·복사·로그에 기록하지 않는다.
- `/opt/stock-app` 운영 파일을 수정하지 않는다.
- PM2 재시작, 운영 배포, 데이터베이스 마이그레이션 적용을 실행하지 않는다.
- `AGENTS.md`와 `/root/stock-app-staging/stock-command-center`를 수정하지 않는다.
- `git reset`, `git clean`, 강제 체크아웃, 기존 사용자 변경 삭제를 실행하지 않는다.

## 작업 원칙
- 사용자가 요청한 기능과 직접 관련된 파일만 수정한다.
- 기존 구조, 인증, 라우팅, UI, 환경변수를 임의로 바꾸지 않는다.
- 현재 저장소의 미커밋 변경사항을 보존하고 덮어쓰거나 되돌리지 않는다.
- 새 기능은 기본적으로 비활성 또는 안전 모드로 작성한다.
- 파일을 수정하기 전에 관련 구현을 충분히 읽고 최소 변경으로 작업한다.
- 임의 패키지 설치, 외부 서비스 가입, 유료 API 사용을 하지 않는다.
- 오류를 숨기거나 테스트를 억지로 통과시키지 않는다.

## 작업 완료 조건
- 변경 파일과 변경 이유를 한국어로 보고한다.
- 실행한 검사와 결과를 한국어로 보고한다.
- 실패한 검사가 있으면 그대로 보고하고 배포 가능한 상태라고 주장하지 않는다.
- 운영 배포는 별도의 사용자 승인 단계에서만 진행한다.
